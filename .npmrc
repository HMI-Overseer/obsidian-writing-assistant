# Obsidian release tags match manifest.json exactly, with no leading "v"
# (the community updater matches the tag to the version verbatim). Force an
# empty prefix so `npm version <patch|minor|major>` tags 1.3.1, not v1.3.1,
# regardless of the user's global npm config.
tag-version-prefix=
