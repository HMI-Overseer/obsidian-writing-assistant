/**
 * Placeholder shown in benchmark reports and summary tables when a metric has
 * no data to display: a score with zero iterations, a duration that was never
 * measured, and similar. Deliberately the literal "N/A" rather than prose
 * punctuation, because it stands in for a missing value in a visible cell, so a
 * comma would be meaningless here.
 */
export const NO_DATA = "N/A";
